#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char y;

	for (y = 'a'; y <= 'z'; x++)
		if (y == 'e' || y == 'q')
			continue;
		else
			putchar(y);
	putchar('\n');
	return (0);
}
